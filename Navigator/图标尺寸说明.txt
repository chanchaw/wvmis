导航页面（不是左侧图标）图标尺寸：
PNG控件引用图片后的尺寸为：800*800（控件会自动调整为795*795）
PNG控件下方的LABEL采用字体为：宋体，粗体，5号（默认为小5）
LABEL和PNG上下间隔为：900
使用QQ聊天框截图的尺寸为：100*100（保证背景色为白色。软件中导航页面背景色也是白色）





报表控件标准属性说明：
使用TextBox无边框，字体同样采用：宋体，粗体，5号（默认为小5）
width:1875,height:255
默认上面的设置可供显示8个中文字符
那么需要换行的话以8个中文为一个行，
换行后每行的高度为255